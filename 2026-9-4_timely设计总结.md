# 逻辑时间约束公式：统一调度抽象

将空间上的资源分配、控制流上的串并行、以及时间上的同步，全部统一抽象为“逻辑时间约束公式”。

## 核心特性

- **调度参数独立化**：不再将调度参数（如并行度 `P`、流水线深度 `LAG`、空间梯度 `ROW/COL`）硬编码进专用的控制流结构（如 `T.Kernel`、`T.Persistent`、`T.serial` 等），而是使其作为独立的一等公民常量进入时间代数公式中；
- **逻辑时间代表偏序**：`t = t0 + f(索引, 参数)` 只定义了“哪个操作先于哪个操作**发射**”；
    - DSL应当同时包括**时间线+依赖关系**，时间线不能取代依赖关系；同时可能存在**资源约束**导致的先后顺序；
        - 设计的DSL应当为依赖关系的识别提供更清晰的语义：类似于Syncopate的通信计划，前端可能需要提供数据依赖Annotations；同时可能要复用，甚至修改triton的依赖分析IR/pass；
        - 后端需要实现同步原语的插入pass；
        - 物理执行时需分配具体资源（SM、线程、共享内存），后端Compiler需要考虑物理资源使用的调度问题；
    - 将逻辑时间解释为**发射偏序**而非严格**执行顺序**；发射后是否执行，由**依赖关系**和**资源约束**确定，依赖关系需要后端引入同步，资源约束需要引入调度；
    - **逻辑时间绝对数值没有物理意义**：如：*仅*有两个操作时， `t->t+1` 和 `t->t+100` 在逻辑上等价；**因此后端需要做“保序稠密化”**；
    - 相同t下，所有Op被允许发射的时间以最早可执行的指令为准；例如AG1/GEMM0，可能AG0时间较长，但AG1已然可以被发起，那么在AG1发起的时候，GEMM0也被发起，但由于其所以来的AG0没有完成，所以必须阻塞等待；
- **固有数据依赖约束合法性**：无论公式如何调整（如 P 变小，导致循环折叠），编译器必须校验物理数据依赖是否满足。如果公式导致读取在写入之前发生，直接报错；
    - 允许时间约束给出的调度结果不是最优的；
    - (Optional，日后再说)应当存在一种机制退还到正常写循环的模式，确保性能不会太差；

## 不同开发路线

Codex给出的建议：
| 路线 | 引入层次 | 主要复用 | 需要开发 | 适用性 |
|---|---|---|---|---|
| 1. DSL→Triton 源码生成 | Triton Python/TTIR 之前 | Triton JIT、autotune、kernel 库 | 时间公式解析、合法性检查、代码模板 | 最快验证 DSL，但 IR 不稳定、诊断和组合能力弱 |
| 2. 独立 TM Dialect→TTIR | TTIR 之前 | MLIR affine/Presburger、接口体系、完整 Triton 后端 | Schedule IR、依赖分析、计划生成、TTIR lowering | **推荐主路线**，语义与硬件解耦 |
| 3. Host Task Graph 路线 | Triton kernel 编译器外围 | Triton kernel、CUDA stream/event/graph、NCCL | Graph/Comm IR、runtime scheduler、跨 kernel buffer 管理 | 最适合 AllGather+GEMM、多 GPU；单 kernel 融合有限 |
| 4. 深入 TTGIR | TTIR→TTGIR 后、pipeline 前 | Triton layout、async copy、pipeline、warp specialization、membar | TTG 调度接口、资源规划、target-specific sync legalization | 性能上限最高，但维护成本和硬件耦合最大 |


## *伪*代码示例(注意，只是逻辑示意，实现不一定按照这个来，比如依赖关系图)

### 计算-通信流水

**修改前：**
```python
@tm.kernel
def allgather_gemm_old(A, B, C):
    with T.Kernel(...) as bid:
        if bid < num_comp_sms: # 计算 SM
            if remote_tile:
                T.wait_get(local_signal_q, workers)  # 显式等待通信完成
            for k in T.Pipelined(...):  # 显式流水线控制
                T.copy(gathered_A[q, k], A_sharded)
                # 必须确保所有 shard 通信完才计算
                T.barrier_arrive(A_ready, phase)
                T.barrier_wait(A_ready, phase) # 显式同步屏障
                T.gemm(A_sharded, B_sharded, C_local)
```

**修改后：**
```python
@tm.kernel
def allgather_gemm_new(A, B, C, LAG: Const):
    # 定义逻辑时间轴和单位时间步长 II
    t0: Time, II: Const
    
    for q in tm.domain(Q):
        # 数据依赖：当前 shard 的逻辑时间起点
        tq = t0 + q * II  
        
        # 通信操作，其完成时间被自动计算
        g = tm.allgather_shard(tm.load(A[q]), tq)
        
        # 核心：逻辑时间约束声明（偏序）。
        # 这代表“GEMM 开始时间”必须在“AllGather 发射时间”之后偏移 LAG 个单位时间
        tc = tq + LAG * II  
        
        # 编译器自动根据 tc 约束，在物理底层生成对应的 wait/signal 或流水线
        c = tm.gemm_shard(g, tm.load(B), tc) 
        
        tm.store(C[q], c)
```

### 遍历顺序

**修改前：**
```python
# A: row-major (串行扫描)
@T.prim_func
def dp_row(H, A):
    with T.Kernel(1, threads=1):
        for i in T.serial(1, M):
            for j in T.serial(1, N):
                update(H, A, i, j) # 显式串行循环

# B: diagonal wavefront (对角线并行)
@T.prim_func
def dp_wave(H, A):
    with T.Kernel(NBLOCKS, threads=B) as bx:
        for d in T.serial(2, M + N - 1):
            for tx in T.Parallel(B):
                i = bx + B
                j = d - i
                if T.all_of(i < M, 1 <= j, j < N):
                    update(H, A, i, j)
            T.sync_grid() # 显式同步，等待整条对角线算完才进入下一条
```

**修改后：**
```python
@tm.func
def dp_cell[t: Time](n, w, a):
    return update(n, w, a)

@tm.kernel
def dp(H, A, t0: Time, ROW: Const, COL: Const):
    # 全域遍历，不再写串行/并行循环
    for i, j in tm.domain((1, M), (1, N)):
        # 核心逻辑时间公式：将空间二维坐标映射为逻辑一维时间
        tij = t0 + i * ROW + j * COL 
        
        # 编译器根据 tij 和下述固有依赖自动推导控制流
        n = tm.load(H[i-1, j]).at(tij)
        w = tm.load(H[i, j-1]).at(tij)
        a = tm.load(A[i, j]).at(tij)
        
        # 调用纯函数 dp_cell
        y = dp_cell(n, w, a)
        
        tm.store(H[i, j], y)
```

### 执行折叠

**修改前：**
```python
# 控制流结构决定一切
with T.Kernel(threads=...):
    for i in T.serial(...):
        for j in T.serial(...):
            ... update(...) 
    T.sync_grid()
# 或者显式写:
T.wait(...); T.signal(...)
```

**修改后：**
```python
@tm.kernel
def tiled_gemm(A, B, C, t0: Time, P: Const, II: Const):
    for q in tm.domain(Q):
        # 极简的纯数学表达，P 只是公式中的普通常数
        tq = t0 + (q // P) * II  
        
        a = tm.load(A[q]).at(tq)
        b = tm.load(B[q]).at(tq)
        
        gemm_tile(a, b) # 纯计算
        
        tm.store(C[q], ...)
```